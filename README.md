# Weather-App
A simple C++ console application that fetches real-time weather data using a REST API. Built with C++ REST SDK (cpprestsdk), it allows users to enter a city name and retrieves the temperature, humidity, and weather conditions using HTTP client requests and JSON parsing.
